import { ShowScrollButtonDirective } from './show-scroll-button.directive';

describe('ShowScrollButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowScrollButtonDirective();
    expect(directive).toBeTruthy();
  });
});
