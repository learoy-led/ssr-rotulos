import { ShowSubmennuDirective } from './show-submennu.directive';

describe('ShowSubmennuDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowSubmennuDirective();
    expect(directive).toBeTruthy();
  });
});
